Module cowait.tasks.agent.agent
===============================

Classes
-------

`Agent(**inputs)`
:   Creates a new instance of the task. Pass inputs as keyword arguments.

    ### Ancestors (in MRO)

    * cowait.tasks.task.Task

    ### Methods

    `destroy(self, task_id) ‑> NoneType`
    :

    `destroy_all(self) ‑> NoneType`
    :

    `emulate_stop(self, task_id: str)`
    :

    `get_agent_url(self) ‑> str`
    :

    `init(self)`
    :

    `list_tasks(self) ‑> list`
    :

    `run(self, **inputs) ‑> dict`
    :