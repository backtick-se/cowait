version: 1
pipeline:
  image: backtickse/sparky-test
  base: backtickse/task-spark

  cluster:
    default: docker
    docker:
      network: tasks
    kubernetes:
      namespace: default
      domain: cluster.backtick.se
